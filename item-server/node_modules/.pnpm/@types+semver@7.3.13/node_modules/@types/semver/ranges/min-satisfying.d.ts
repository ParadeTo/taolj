import Range = require('.pnpm/@types+semver@7.3.13/node_modules/@types/semver/classes/range');
import SemVer = require('.pnpm/@types+semver@7.3.13/node_modules/@types/semver/classes/semver');
import semver = require('.pnpm/@types+semver@7.3.13/node_modules/@types/semver');

/**
 * Return the lowest version in the list that satisfies the range, or null if none of them do.
 */
declare function minSatisfying<T extends string | SemVer>(
    versions: ReadonlyArray<T>,
    range: string | Range,
    optionsOrLoose?: boolean | semver.RangeOptions,
): T | null;

export = minSatisfying;
